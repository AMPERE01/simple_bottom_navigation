import 'package:flutter/material.dart';

class DataPage_Screen extends StatelessWidget {
  const DataPage_Screen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Data Page"),
      ),
    );
  }
}