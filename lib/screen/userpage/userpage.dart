import 'package:flutter/material.dart';

class UserPage_Screen extends StatelessWidget {
  const UserPage_Screen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Profile Page"),
      ),
    );
  }
}