import 'package:flutter/material.dart';

class CoinPage_Screen extends StatelessWidget {
  const CoinPage_Screen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Coin Page"),
      ),
    );
  }
}